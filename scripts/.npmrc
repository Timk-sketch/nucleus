# Fail the install when the runtime does not satisfy engines.node, instead of
# printing a warning and letting the job crash later at import/runtime.
# @supabase/supabase-js >= 2.78 requires Node >= 22; the workflows pinned Node 20
# and npm install happily proceeded. This turns that class of drift into a hard stop.
engine-strict=true
